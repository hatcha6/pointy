# The backend the LAN front door is currently sending traffic to.
#
# GENERATED FILE — rewritten by update.sh during a live update, and reset to
# the managed backend by install.sh and by watchdog.sh whenever the
# container named here is not running. Deleting it is safe: the image's built-in
# default is this same managed backend.
set $pointy_upstream      "http://backend:8000";
set $pointy_upstream_name "backend";
