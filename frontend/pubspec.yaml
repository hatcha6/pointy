name: pointy_frontend
description: "Flutter point-of-sale frontend for Daftar (دفتر)."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.7

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  # Barcode scan feedback chimes (POS/purchasing): success, not-found, error.
  # Linux playback uses GStreamer — the release workflow installs the dev
  # headers for `flutter build linux`.
  audioplayers: ^6.1.0
  # Disk-cached product images: honors the backend's Cache-Control/ETag so a
  # catalog scroll-back or app restart doesn't re-download every thumbnail.
  cached_network_image: ^3.4.1
  http: ^1.2.2
  intl: any
  archive: ^4.0.9
  fl_chart: ^1.2.0
  flutter_libserialport: ^0.6.0
  esc_pos_utils_plus: ^2.0.4
  image: ^4.3.0
  flutter_bluetooth_classic_serial: ^1.3.2
  multicast_dns: ^0.3.3
  shared_preferences: ^2.5.5
  mobile_scanner: ^7.2.0
  pdf: ^3.12.0
  printing: ^5.14.3
  qr: ^3.0.2
  file_picker: ^11.0.2
  http_parser: ^4.1.2
  web: ^1.1.1
  usb_serial: ^0.5.2
  image_picker: ^1.2.2
  gpt_markdown: ^1.1.7
  # Open web-search source citations (http/https links in AI replies) externally.
  url_launcher: ^6.3.1
  record: ^6.2.1
  # Client self-update: read the running app version + a temp dir for the download.
  package_info_plus: ^8.0.0
  path_provider: ^2.1.4
  wakelock_plus: ^1.3.3
  flutter_tts: ^4.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates per-platform launcher/app icons from assets/branding/logo.png.
  # Run `flutter pub run flutter_launcher_icons` after changing the logo.
  flutter_launcher_icons: ^0.14.4

# App icon generation for all platforms. Source: assets/branding/logo.png.
flutter_launcher_icons:
  image_path: "assets/branding/logo.png"
  # Android: legacy icon + adaptive icon (logo on a white background).
  android: "ic_launcher"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/branding/logo.png"
  # iOS / macOS require opaque icons; flatten transparency onto white.
  ios: true
  remove_alpha_ios: true
  background_color_ios: "#FFFFFF"
  macos:
    generate: true
    image_path: "assets/branding/logo.png"
  windows:
    generate: true
    image_path: "assets/branding/logo.png"
    icon_size: 256
  web:
    generate: true
    image_path: "assets/branding/logo.png"
    background_color: "#FFFFFF"
    theme_color: "#FFFFFF"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  fonts:
    - family: IBMPlexSansArabic
      fonts:
        - asset: assets/fonts/IBMPlexSansArabic-Regular.ttf
          weight: 400
        - asset: assets/fonts/IBMPlexSansArabic-Medium.ttf
          weight: 500
        - asset: assets/fonts/IBMPlexSansArabic-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/IBMPlexSansArabic-Bold.ttf
          weight: 700

  # Also exposed as plain assets so PDF generation can read the raw font bytes
  # via rootBundle and hand them to a background isolate.
  assets:
    - assets/fonts/IBMPlexSansArabic-Regular.ttf
    - assets/fonts/IBMPlexSansArabic-Bold.ttf
    # Brand logo, shown in-app (e.g. the login screen) and used to generate
    # the per-platform launcher icons via flutter_launcher_icons.
    - assets/branding/logo.png
    # Monochrome brand mark used inside the printed "دُوِّنَ في دفتر" tagline
    # (PDF documents + thermal receipts). Read as raw bytes for print rendering.
    - assets/branding/logo_black.png
    # Barcode scan feedback chimes (see shared/barcode/scan_feedback_sounds.dart).
    - assets/sounds/

  # To add more assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
